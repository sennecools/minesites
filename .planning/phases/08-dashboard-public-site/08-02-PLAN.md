---
phase: 08-dashboard-public-site
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[subdomain]/page.tsx
  - src/app/[subdomain]/layout.tsx
  - src/app/[subdomain]/preview-client.tsx
  - src/components/preview/types.ts
  - src/components/site/nav.tsx
  - src/components/sections/hero-section.tsx
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "Visiting [subdomain].minesites.net renders the Website's published sections"
    - "`serverIp` is no longer plumbed through the public-site layer"
    - "Drafts (published === false) still 404 on the public URL"
    - "TypeScript compiles with zero errors after the prop-shape narrow"
  artifacts:
    - path: "src/components/preview/types.ts"
      provides: "WebsiteData shape without serverIp"
      contains: "export interface WebsiteData"
    - path: "src/components/site/nav.tsx"
      provides: "SiteNav with single `serverName` prop (no Copy IP button)"
      contains: "interface SiteNavProps"
    - path: "src/app/[subdomain]/layout.tsx"
      provides: "Public layout with no `serverIp` placeholder"
    - path: "src/app/[subdomain]/page.tsx"
      provides: "RSC that passes name+subdomain only to PreviewClient"
  key_links:
    - from: "src/app/[subdomain]/page.tsx"
      to: "db.website.findUnique({ where: { subdomain } })"
      via: "Prisma query"
      pattern: "db\\.website\\.findUnique"
    - from: "src/app/[subdomain]/layout.tsx"
      to: "<SiteNav serverName={...} />"
      via: "JSX prop pass"
      pattern: "<SiteNav serverName"
---

<objective>
Drop the legacy `serverIp` plumbing from the public-site layer per D-14 + D-18: `WebsiteData.serverIp` is removed from the shared preview types, `[subdomain]/page.tsx` stops passing `serverIp: null` to `PreviewClient`, `[subdomain]/layout.tsx` drops the `serverIp = ""` placeholder, and `SiteNav` drops the `serverIp` prop + Copy IP button. Also confirm `[subdomain].minesites.net` still renders correctly (success criterion 4 verification) and clean up the dead `serverIp` references in `hero-section.tsx` so the TypeScript compile gate stays green.

Purpose: Closes success criterion 4 of Phase 8 (verify the public route works after the model rename) plus the D-14 cleanup. The future SECT-02/SECT-03 section types will re-introduce the IP display via a section, not via the layout nav.

Output:
- `WebsiteData.serverIp` removed from `src/components/preview/types.ts`
- `serverIp: null` removed from the `serverData` object in `[subdomain]/page.tsx`
- `serverIp` placeholder + prop removed from `[subdomain]/layout.tsx`
- `SiteNav` simplified to `serverName` only — Copy IP button + related state removed
- `hero-section.tsx` cleared of its `serverIp` references (orphaned consumer)
- `preview-client.tsx` verified clean (drops any internal `serverIp` reads it had)
- TypeScript compile gate passes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dashboard-public-site/08-CONTEXT.md
@.planning/phases/08-dashboard-public-site/08-RESEARCH.md
@.planning/phases/08-dashboard-public-site/08-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Verified file contents 2026-05-12. -->

From src/components/preview/types.ts:
```typescript
export interface WebsiteData {
  name: string;
  subdomain: string;
  serverIp: string | null;   // DROP
  players?: number;
  maxPlayers?: number;
  version?: string;
}
```

From src/app/[subdomain]/page.tsx line 35 (inside serverData object passed to PreviewClient):
```typescript
const serverData = {
  name: server.name,
  subdomain: server.subdomain,
  serverIp: null as string | null,   // DROP
};
```

From src/app/[subdomain]/layout.tsx lines 77 + 106:
```typescript
const serverIp = "";   // Phase 6 placeholder; Phase 7 adds MinecraftServer lookup  ← DROP
// ...
<SiteNav serverName={serverName} serverIp={serverIp} />   // becomes <SiteNav serverName={serverName} />
```

From src/components/site/nav.tsx (lines 4-49 — verified 2026-05-12):
```typescript
"use client";

import { useState } from "react";
import { Copy, Check } from "lucide-react";

interface SiteNavProps {
  serverName: string;
  serverIp: string;          // DROP
}

export function SiteNav({ serverName, serverIp }: SiteNavProps) {
  const [copied, setCopied] = useState(false);
  const handleCopy = async () => {
    if (!serverIp) return;
    try {
      await navigator.clipboard.writeText(serverIp);
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    } catch { /* clipboard unavailable */ }
  };

  return (
    <nav ...>
      <span ...>{serverName}</span>
      <button onClick={handleCopy} ... aria-label="Copy server IP">
        {copied ? <Check /> : <Copy />}
        {copied ? "Copied!" : "Copy IP"}
      </button>
    </nav>
  );
}
```

From src/components/sections/hero-section.tsx (lines 10, 13, 58, 70, 75 — confirmed orphaned consumer):
```typescript
interface HeroSectionProps {
  // ...
  serverIp?: string | null;   // DROP (this component is exported via sections/index.ts but PreviewClient routes through hero-render.tsx in the registry path)
}

export function HeroSection({ title, subtitle, settings, serverIp }: HeroSectionProps) {
  // lines 58-78 render an IP+Copy block when serverIp is truthy — DROP the entire block
}
```
This file is exported from `src/components/sections/index.ts` so removing the prop is a public API change; we must verify no caller passes `serverIp` (per RESEARCH §Open Question 4, no active caller is expected). After this plan it cleanly compiles without the prop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip serverIp from the shared WebsiteData type and the [subdomain] RSC</name>
  <files>
    src/components/preview/types.ts,
    src/app/[subdomain]/page.tsx,
    src/app/[subdomain]/preview-client.tsx
  </files>
  <read_first>
    - src/components/preview/types.ts (full file — verify the only change is dropping line 6 `serverIp`)
    - src/app/[subdomain]/page.tsx (lines 1-60, especially line 32-37 the `serverData` object literal; lines 15-30 the `db.website.findUnique` call MUST be preserved as-is per D-17)
    - src/app/[subdomain]/preview-client.tsx (grep for `serverIp` — verify whether the component destructures or reads `serverData.serverIp` anywhere)
    - .planning/phases/08-dashboard-public-site/08-CONTEXT.md (D-14, D-17, D-18)
    - .planning/phases/08-dashboard-public-site/08-RESEARCH.md §Common Pitfalls (pitfalls 6 — noreferrer noopener — not relevant here; this task does NOT add new anchors; just verify carry-forward)
  </read_first>
  <action>
    **(a) `src/components/preview/types.ts` — drop one line:**
    ```diff
     export interface WebsiteData {
       name: string;
       subdomain: string;
    -  serverIp: string | null;
       players?: number;
       maxPlayers?: number;
       version?: string;
     }
    ```
    No other types in the file change. `Section`, `StatsServer`, `FeatureItem`, `GalleryImage`, `isColorDark`, `isLightColor` remain untouched.

    **(b) `src/app/[subdomain]/page.tsx` — drop `serverIp: null` from the `serverData` literal (line ~35):**
    ```diff
       const serverData = {
         name: server.name,
         subdomain: server.subdomain,
    -    serverIp: null as string | null,
       };
    ```
    Do NOT touch the `db.website.findUnique` query (line ~15-24) or the `notFound()` gate (line ~27) — D-17 says these must be preserved verbatim. Do NOT touch the published-or-preview-mode logic.

    **(c) `src/app/[subdomain]/preview-client.tsx` — verify clean:**
    - Run `grep -n "serverIp" src/app/[subdomain]/preview-client.tsx` first.
    - If zero matches: no edit needed (the prop type is imported from `WebsiteData` which now drops the field, and TypeScript will narrow automatically).
    - If matches exist (any `serverData.serverIp` reads, any local destructure `{ serverIp }`): remove every such reference. Do NOT add `?? null` or `?? ""` fallbacks — drop them outright. The point of D-14 is to eliminate the prop.
    - The internal switch-case section dispatch (PreviewClient is long, ~700+ lines) MUST be otherwise unchanged. Only `serverIp` references go.

    **(d) Confirm Phase 7 carry-forwards in `[subdomain]/page.tsx` are still intact:**
    - The `findUnique({ where: { subdomain }, include: { sections: { where: { visible: true } } } })` shape is unchanged.
    - The `notFound()` gate `if (!server || (!server.published && !isPreviewMode))` is unchanged.
    - No new dependencies added to the file.
  </action>
  <verify>
    <automated>
    # 1. WebsiteData no longer contains serverIp
    test "$(grep -c 'serverIp' src/components/preview/types.ts)" = "0"
    # 2. [subdomain]/page.tsx no longer carries serverIp
    test "$(grep -c 'serverIp' 'src/app/[subdomain]/page.tsx')" = "0"
    # 3. notFound() gate preserved
    grep -q "notFound()" "src/app/[subdomain]/page.tsx"
    # 4. preview-client.tsx no longer references serverIp
    test "$(grep -c 'serverIp' 'src/app/[subdomain]/preview-client.tsx')" = "0"
    # 5. TypeScript still compiles
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - `WebsiteData` interface contains exactly: `name`, `subdomain`, optional `players`/`maxPlayers`/`version`. No `serverIp`.
    - `[subdomain]/page.tsx` `serverData` object is `{ name, subdomain }` only.
    - `[subdomain]/page.tsx` still calls `db.website.findUnique` and `notFound()` for unpublished non-preview requests.
    - `preview-client.tsx` carries zero `serverIp` text occurrences.
    - `npx tsc --noEmit` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "serverIp" src/components/preview/types.ts` returns 0.
    - `grep -c "serverIp" "src/app/[subdomain]/page.tsx"` returns 0.
    - `grep -c "serverIp" "src/app/[subdomain]/preview-client.tsx"` returns 0.
    - `grep -c "notFound()" "src/app/[subdomain]/page.tsx"` returns >= 1.
    - `grep -c "db.website.findUnique" "src/app/[subdomain]/page.tsx"` returns >= 1.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Drop serverIp from the public layout, SiteNav, and the orphaned hero-section consumer</name>
  <files>
    src/app/[subdomain]/layout.tsx,
    src/components/site/nav.tsx,
    src/components/sections/hero-section.tsx
  </files>
  <read_first>
    - src/app/[subdomain]/layout.tsx (full file, especially lines 70-110 where the `serverIp = ""` placeholder and `<SiteNav>` mount live)
    - src/components/site/nav.tsx (full file — small, lines 1-55)
    - src/components/sections/hero-section.tsx (full file, especially lines 1-15 props interface and lines 55-80 the IP+Copy display block)
    - src/components/sections/index.ts (verify what is exported from this barrel — `HeroSection` is currently re-exported)
    - .planning/phases/08-dashboard-public-site/08-CONTEXT.md (D-14 option (a) explicitly chosen — drop prop)
    - .planning/phases/08-dashboard-public-site/08-PATTERNS.md §`src/components/site/nav.tsx`
    - .planning/phases/08-dashboard-public-site/08-RESEARCH.md §Assumption A7 (hero-section.tsx is likely orphaned; verify by grep before deciding scope)
  </read_first>
  <action>
    **(a) `src/app/[subdomain]/layout.tsx` — drop the placeholder var and the prop pass (D-14 option (a)):**
    ```diff
       const serverName = server?.name ?? subdomain;
    -  const serverIp = "";   // Phase 6 placeholder; Phase 7 adds MinecraftServer lookup
    ```
    ```diff
    -      <SiteNav serverName={serverName} serverIp={serverIp} />
    +      <SiteNav serverName={serverName} />
    ```
    All other lines in the layout (font loading, `.site-root` wrapper, theme CSS-variable injection) MUST remain unchanged — CSS isolation per CLAUDE.md rule 2.

    **(b) `src/components/site/nav.tsx` — drop the prop, the Copy IP button, and the clipboard state:**
    ```diff
     "use client";

    -import { useState } from "react";
    -import { Copy, Check } from "lucide-react";
    -
    -interface SiteNavProps {
    -  serverName: string;
    -  serverIp: string;
    -}
    +interface SiteNavProps {
    +  serverName: string;
    +}

    -export function SiteNav({ serverName, serverIp }: SiteNavProps) {
    -  const [copied, setCopied] = useState(false);
    -
    -  const handleCopy = async () => {
    -    if (!serverIp) return;
    -    try {
    -      await navigator.clipboard.writeText(serverIp);
    -      setCopied(true);
    -      setTimeout(() => setCopied(false), 2000);
    -    } catch {
    -      // Clipboard unavailable — fail silently
    -    }
    -  };
    -
    +export function SiteNav({ serverName }: SiteNavProps) {
       return (
         <nav className="sticky top-0 z-50 h-14 flex items-center justify-between px-6"
              style={{ backgroundColor: "var(--site-card)" }}>
           <span className="font-bold text-lg"
                 style={{ fontFamily: "var(--site-font-display)", color: "var(--site-text)" }}>
             {serverName}
           </span>
    -      <button onClick={handleCopy} ... aria-label="Copy server IP">
    -        {copied ? <Check className="w-4 h-4" /> : <Copy className="w-4 h-4" />}
    -        {copied ? "Copied!" : "Copy IP"}
    -      </button>
         </nav>
       );
     }
    ```
    The CSS variable styling (`var(--site-card)`, `var(--site-font-display)`, `var(--site-text)`) is preserved — these are the `.site-root`-scoped tokens. No dashboard tokens leak in.

    **(c) `src/components/sections/hero-section.tsx` — drop the orphaned serverIp consumer:**
    First confirm with `grep -rn "from.*hero-section\|from \"./hero-section\"" src/` that the only importer is `src/components/sections/index.ts` (the registry path goes through `hero-render.tsx`, not `hero-section.tsx`).
    Then:
    ```diff
     interface HeroSectionProps {
       title: string | null;
       subtitle: string | null;
       settings: ... ;
    -  serverIp?: string | null;
     }

    -export function HeroSection({ title, subtitle, settings, serverIp }: HeroSectionProps) {
    +export function HeroSection({ title, subtitle, settings }: HeroSectionProps) {
    ```
    AND delete the entire conditional IP+Copy block (currently lines ~58-78):
    ```diff
    -        {serverIp && (
    -          <div ...>
    -            <span ...>{serverIp}</span>
    -            <button onClick={() => navigator.clipboard.writeText(serverIp)} ... />
    -          </div>
    -        )}
    ```
    The remaining hero render (title + subtitle + background settings) is unchanged. Do NOT delete the file; do NOT change its export in `sections/index.ts`.

    **(d) Phase 7 carry-forward preservation check:** None of the changes touch API routes, `actions.ts`, validation schemas, or middleware. No carry-forward is at risk in this task.
  </action>
  <verify>
    <automated>
    # 1. Layout no longer references serverIp
    test "$(grep -c 'serverIp' 'src/app/[subdomain]/layout.tsx')" = "0"
    # 2. SiteNav has only one prop and no clipboard logic
    test "$(grep -c 'serverIp' src/components/site/nav.tsx)" = "0"
    test "$(grep -c 'navigator.clipboard' src/components/site/nav.tsx)" = "0"
    test "$(grep -c 'aria-label=\"Copy server IP\"' src/components/site/nav.tsx)" = "0"
    grep -q "interface SiteNavProps" src/components/site/nav.tsx
    # 3. Hero section drops the serverIp prop and copy-IP block
    test "$(grep -c 'serverIp' src/components/sections/hero-section.tsx)" = "0"
    # 4. The .site-root wrapper still exists in layout.tsx (CSS isolation preserved)
    grep -q "site-root" "src/app/[subdomain]/layout.tsx"
    # 5. TypeScript compile gate
    npx tsc --noEmit
    # 6. Next.js build sanity (the public route shouldn't fail to build)
    npx next build --no-lint 2>&1 | tail -20 || true
    </automated>
  </verify>
  <done>
    - `[subdomain]/layout.tsx` no longer declares a `serverIp` placeholder; `<SiteNav>` receives only `serverName`.
    - `SiteNav` has a single-prop interface and no clipboard state.
    - `hero-section.tsx` has no `serverIp` references and no IP+Copy display block.
    - `.site-root` wrapper preserved in the public layout (CSS isolation intact).
    - `npx tsc --noEmit` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -c "serverIp" "src/app/[subdomain]/layout.tsx"` returns 0.
    - `grep -c "<SiteNav serverName=" "src/app/[subdomain]/layout.tsx"` returns >= 1, AND that line does NOT contain ` serverIp=` (grep refinement: `grep "<SiteNav " "src/app/[subdomain]/layout.tsx" | grep -c "serverIp"` returns 0).
    - `grep -c "serverIp" src/components/site/nav.tsx` returns 0.
    - `grep -c "navigator.clipboard" src/components/site/nav.tsx` returns 0.
    - `grep -c 'aria-label="Copy server IP"' src/components/site/nav.tsx` returns 0.
    - `grep -c "interface SiteNavProps" src/components/site/nav.tsx` returns 1.
    - `grep -c "serverIp" src/components/sections/hero-section.tsx` returns 0.
    - `grep -c "site-root" "src/app/[subdomain]/layout.tsx"` returns >= 1.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor -> `[subdomain].minesites.net` | Unauthenticated. Middleware rewrites the host to `/[subdomain]`. `db.website.findUnique` filters by subdomain; `notFound()` if unpublished or missing. The path is read-only; no write surface is touched in this plan. |
| `.site-root` CSS scope vs. dashboard | Hard boundary established in Phase 2. This plan must not modify the wrapper or its CSS tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information disclosure | Removing `notFound()` gate would expose unpublished Websites' content | mitigate | Acceptance criteria explicitly verifies `notFound()` and `db.website.findUnique` still exist in `[subdomain]/page.tsx`. The `published || isPreviewMode` branch is NOT touched. |
| T-08-06 | Tampering | CSS isolation break — dashboard tokens leak into `.site-root` scope | mitigate | The `.site-root` wrapper class on `[subdomain]/layout.tsx` is preserved (acceptance criterion verifies). No new `--background`/`--foreground`/dashboard tokens are added to the layout. The Copy IP button removal is a pure deletion — no new tokens introduced. |
| T-08-07 | Tampering (stored XSS) | `serverName` (user-controlled `Website.name`) rendered in `<span>{serverName}</span>` of `SiteNav` | mitigate | React text-interpolation auto-encodes. No `dangerouslySetInnerHTML` exists in nav.tsx. RESEARCH §Security Domain confirms safe. |
| T-08-08 | Information disclosure | Phase 7 carry-forwards (subdomain 409 on PUT, freemium gate, P2002 catch, user existence check, ownership chain) | accept | None of this plan's files touch API routes, server actions, or validation. The carry-forwards live in `src/app/api/websites/*` and `src/app/(dashboard)/dashboard/actions.ts` — both out of this plan's diff. |
| T-08-09 | DoS | Build failure due to leftover `serverIp` reads in `hero-section.tsx` after the type drops | mitigate | Task 2 explicitly cleans the orphaned consumer and the verify step runs `npx tsc --noEmit`. |
</threat_model>

<verification>
- Compile gate: `npx tsc --noEmit` exits 0.
- Grep gate: zero `serverIp` text occurrences in: `preview/types.ts`, `[subdomain]/page.tsx`, `[subdomain]/layout.tsx`, `preview-client.tsx`, `site/nav.tsx`, `hero-section.tsx`.
- `.site-root` wrapper preserved in `[subdomain]/layout.tsx`.
- `notFound()` + `db.website.findUnique` preserved in `[subdomain]/page.tsx` (D-17 verification).
- Manual smoke (executor discretion): visit a published Website's subdomain in dev, confirm the page renders with the Website name in the nav and no Copy IP button. Visit an unpublished Website's subdomain, confirm 404.
</verification>

<success_criteria>
- DASH-04 success criterion 4 holds: `[subdomain].minesites.net` continues to render the Website's published sections via the existing path (verified by build success + manual smoke).
- All `serverIp` text occurrences purged from this plan's `files_modified`.
- TypeScript compile gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-public-site/08-02-SUMMARY.md` covering:
- The 6 files modified and the lines dropped from each
- Confirmation that `.site-root` + `notFound()` + `db.website.findUnique` are intact
- TypeScript gate result
- Smoke-test result (subdomain page renders, draft 404s)
- Any unexpected `serverIp` hit (e.g., in `preview-client.tsx`) and how it was resolved
</output>
