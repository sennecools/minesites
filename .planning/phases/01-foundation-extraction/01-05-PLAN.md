---
phase: 01-foundation-extraction
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
    - src/app/[subdomain]/preview-client.tsx
autonomous: true
requirements: []
tags:
    - public-renderer
    - cleanup
    - registry-wireup
must_haves:
    truths:
        - 'preview-client.tsx imports SECTION_REGISTRY from @/lib/section-registry'
        - 'preview-client.tsx imports Section, ServerData, StatsServer, FeatureItem, GalleryImage, isLightColor, isColorDark from @/components/preview/types — local duplicate definitions are DELETED (RESEARCH Open Question #2 recommendation)'
        - "The hero case in the switch dispatcher uses SECTION_REGISTRY['hero'].render — not the local PreviewHero"
        - 'The local function PreviewHero (~113 lines) is DELETED'
        - 'The local mockData constant is DELETED — players/version come from props or registry-resolved component'
        - 'Public site URLs ([subdomain].minesites.net or /[subdomain]) render the hero section identically to the pre-change version (same JSX output)'
        - 'preview-client.tsx line count decreases by at least 130 lines (was 947; target ≤ 817)'
        - 'npx tsc --noEmit and npm run lint pass with no new errors; npm run build succeeds'
    artifacts:
        - path: 'src/app/[subdomain]/preview-client.tsx'
          provides: 'Public renderer with registry-driven Hero, deduplicated types, no mock data'
          contains: 'SECTION_REGISTRY'
    key_links:
        - from: 'src/app/[subdomain]/preview-client.tsx'
          to: 'src/lib/section-registry'
          via: 'named import'
          pattern: "import \\{ SECTION_REGISTRY \\} from '@/lib/section-registry'"
        - from: 'src/app/[subdomain]/preview-client.tsx'
          to: 'src/components/preview/types'
          via: 'type imports + utility imports'
          pattern: "from '@/components/preview/types'"
---

<objective>
Wire the public renderer (`src/app/[subdomain]/preview-client.tsx`) to the registry, dedupe its local type definitions against `@/components/preview/types`, and remove the now-redundant `PreviewHero` and `mockData` (per D-05 and RESEARCH Open Question #2).

This plan ships ROADMAP success criterion #1 ("Hero is registered there and renders identically to before") at runtime — once this plan and plan 04 are both complete, both the editor preview and the public site flow Hero rendering through the same `SECTION_REGISTRY['hero'].render` component, eliminating the duplication that was the original problem.

Output: 1 modified file. Zero new files. The public site still renders identically to the pre-change version (verified visually by the executor — see verification section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-extraction/01-CONTEXT.md
@.planning/phases/01-foundation-extraction/01-RESEARCH.md
@.planning/phases/01-foundation-extraction/01-PATTERNS.md
@.planning/phases/01-foundation-extraction/01-01-SUMMARY.md
@.planning/phases/01-foundation-extraction/01-02-SUMMARY.md
@.planning/phases/01-foundation-extraction/01-03-SUMMARY.md
@CLAUDE.md
@src/types/sections.ts
@src/lib/section-registry.tsx
@src/components/sections/render/hero-render.tsx
@src/components/preview/types.ts
@src/app/[subdomain]/preview-client.tsx

<interfaces>
<!-- What the registry provides (from plan 03) -->
```typescript
export const SECTION_REGISTRY: Record<SectionType, RegistryEntry>;
// SECTION_REGISTRY['hero'].render is HeroRender, signature: ({ section, serverData }) => JSX
```

<!-- What @/components/preview/types exports (existing, from earlier work) -->

```typescript
// src/components/preview/types.ts
export interface ServerData {
	name: string;
	subdomain: string;
	serverIp: string | null;
	players?: number;
	maxPlayers?: number;
	version?: string;
}
export interface Section {
	id: string;
	type: string;
	title: string | null;
	subtitle: string | null;
	settings: Record<string, unknown>;
	visible: boolean;
}
export interface StatsServer {
	id;
	name;
	address?;
	players?;
	maxPlayers?;
	status?;
}
export interface FeatureItem {
	title;
	description;
	icon;
}
export interface GalleryImage {
	id;
	url;
	label?;
}
export function isColorDark(hex: string): boolean;
export function isLightColor(hex: string): boolean;
```

<!-- Current state of preview-client.tsx (line numbers verified by direct grep on 2026-05-07) -->

- Line 1: `"use client";` ← KEEP
- Line 22-29: local `interface Section { ... }` ← DELETE (use import from preview/types)
- Line 31-35: local `interface ServerData { ... }` ← DELETE (use import); NOTE: the local version OMITS players/maxPlayers/version which the canonical version has — switching to the canonical type is a strict improvement
- Line 37-43: local `interface StatsServer { ... }` ← DELETE (use import)
- Line 45-49: local `interface FeatureItem { ... }` ← DELETE (use import)
- Line 51-55: local `interface GalleryImage { ... }` ← DELETE (use import)
- Line 58-62: `const mockData = { players, maxPlayers, version }` ← DELETE
- Line 65-74: local `function isColorDark(hex)` ← DELETE (use import)
- Line 76-85: local `function isLightColor(hex)` ← DELETE (use import)
- Line 109-221: `function PreviewHero({ section, serverData })` ← DELETE (registry handles it)
- Line 232: `version = mockData.version,` (in PreviewStats destructure default) ← REPLACE with `version = "1.20.4"` literal default; PreviewStats stays in this file (not in Phase 1 scope)
- Line 918-919: `case "hero": return <PreviewHero key={section.id} section={section} serverData={server} />;` ← REPLACE with registry lookup
- Line 906: `export default function PreviewClient({ server, sections, isPreviewMode })` ← signature stays
- Other lines ~140-150 (PlayerBadge inner uses mockData.players ×4) ← gone with PreviewHero deletion

<!-- The replacement registry-lookup snippet for the hero case -->

```typescript
case "hero": {
  const Entry = SECTION_REGISTRY["hero"];
  return <Entry.render key={section.id} section={section} serverData={server} />;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace local type duplicates and utility duplicates with imports from @/components/preview/types; delete mockData constant</name>
  <files>src/app/[subdomain]/preview-client.tsx</files>
  <read_first>
    - src/app/[subdomain]/preview-client.tsx (FULL FILE — every reference to local types and mockData must be located before deletion)
    - src/components/preview/types.ts (canonical exports — confirm signatures match exactly before deleting locals)
    - .planning/phases/01-foundation-extraction/01-PATTERNS.md (preview-client.tsx changes table)
    - .planning/phases/01-foundation-extraction/01-RESEARCH.md (Open Question #2 — recommendation: dedupe in Phase 1)
  </read_first>
  <behavior>
    - Test 1: `grep -c "^interface Section" preview-client.tsx` outputs 0 (local Section interface deleted)
    - Test 2: `grep -c "^interface ServerData" preview-client.tsx` outputs 0
    - Test 3: `grep -c "^interface StatsServer" preview-client.tsx` outputs 0
    - Test 4: `grep -c "^interface FeatureItem" preview-client.tsx` outputs 0
    - Test 5: `grep -c "^interface GalleryImage" preview-client.tsx` outputs 0
    - Test 6: `grep -c "^function isColorDark" preview-client.tsx` outputs 0
    - Test 7: `grep -c "^function isLightColor" preview-client.tsx` outputs 0
    - Test 8: `grep -c "const mockData" preview-client.tsx` outputs 0
    - Test 9: A single import line exists pulling all needed names from `@/components/preview/types`
    - Test 10: tsc --noEmit produces no new errors
  </behavior>
  <action>
**Goal:** Eliminate the type duplication between `preview-client.tsx` (lines 22-55) and `@/components/preview/types`. Per RESEARCH Open Question #2 recommendation, this dedupe is in scope for Phase 1.

**Step 1 — Add the consolidated import** at the top of preview-client.tsx, immediately after the existing lucide-react import block (which currently spans lines 3-19).

Insert this single import line:

```typescript
import {
	isColorDark,
	isLightColor,
	type FeatureItem,
	type GalleryImage,
	type Section,
	type ServerData,
	type StatsServer,
} from '@/components/preview/types';
```

The `type` modifier inside the named import is the inline syntax — TypeScript compiles it correctly and ESLint (under typescript-eslint) accepts it. Single-line per-import syntax (`import type { Section } from ...`) is also acceptable; the multi-import form above is preferred for readability.

**Step 2 — Delete the local interface declarations**:

- Lines 22-29: `interface Section { ... }` — DELETE (and the preceding `// Types` comment if any)
- Lines 31-35: `interface ServerData { ... }` — DELETE
- Lines 37-43: `interface StatsServer { ... }` — DELETE
- Lines 45-49: `interface FeatureItem { ... }` — DELETE
- Lines 51-55: `interface GalleryImage { ... }` — DELETE

After deletion, the imports section sits cleanly above the next code block.

**Step 3 — Delete the local utility functions**:

- Lines 65-74: `function isColorDark(hex: string): boolean { ... }` — DELETE
- Lines 76-85: `function isLightColor(hex: string): boolean { ... }` — DELETE
- Any preceding `// Utility functions` comment — DELETE

These names are now provided by the import from `@/components/preview/types`.

**Step 4 — Delete `const mockData`** (lines 58-62 in the pre-edit file):

```typescript
// Mock data for preview
const mockData = {
	players: 247,
	maxPlayers: 500,
	version: '1.20.4',
};
```

DELETE entirely, including the comment.

After deletion, `mockData.version` (line 232 inside PreviewStats destructure default) is now broken. Fix it in the same step:

Find:

```typescript
version = mockData.version,
```

Replace with:

```typescript
version = "1.20.4",
```

(In Phase 3 the Stats section will be re-extracted with proper version handling. For Phase 1 a literal default mirrors the prior fallback semantics — the section displays "1.20.4" only if the user has not configured a stats `version` setting. PreviewStats does not have access to `serverData` in its current signature; threading serverData through is out of Phase 1 scope per D-04.)

**Step 5 — `mockData.players` cleanup** — The 4 references at lines 143, 152, 165, 174 are inside `PreviewHero`, which is deleted in Task 2. After Task 2 completes, these references are gone. After Task 1 alone, leaving them temporarily would create transient compile errors — **so the order matters**: Task 2 (PreviewHero deletion) MUST be performed in the same plan execution session, but the file edits can be done in either order as long as the executor verifies tsc passes only at the end of the plan.

To avoid transient compile errors during the edit cycle, perform Task 1's deletion of `mockData` AFTER Task 2's deletion of `PreviewHero` — or perform both deletions in a single edit pass before running tsc. Either ordering produces the same final file; just don't run `npx tsc --noEmit` between them.

**Step 6 — Confirm import correctness:**

- The `Section` and `ServerData` shapes from `@/components/preview/types` are identical to the deleted locals (RESEARCH confirms zero compatibility risk for Section, FeatureItem, GalleryImage; ServerData in the canonical version has additional optional `players`/`maxPlayers`/`version` fields which is a strict superset — no breakage).
- `isColorDark` and `isLightColor` signatures and behavior in `@/components/preview/types` are identical to the local versions (verified by grep diff).

**Step 7 — Verify line count drop**:

```bash
NEW=$(wc -l < 'src/app/[subdomain]/preview-client.tsx')
echo "DELTA=$(($NEW - 947))"   # Task 1 alone should remove ~45-55 lines (5 interfaces + 2 functions + mockData + comments)
```

  </action>
  <verify>
    <automated>[ "$(grep -cE '^interface Section\\s*\\{' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && [ "$(grep -cE '^interface ServerData\\s*\\{' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && [ "$(grep -cE '^interface StatsServer\\s*\\{' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && [ "$(grep -cE '^interface FeatureItem\\s*\\{' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && [ "$(grep -cE '^interface GalleryImage\\s*\\{' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && [ "$(grep -cE '^function isColorDark\\(' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && [ "$(grep -cE '^function isLightColor\\(' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && [ "$(grep -c 'const mockData' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && grep -c "from '@/components/preview/types'" 'src/app/[subdomain]/preview-client.tsx' | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^interface (Section|ServerData|StatsServer|FeatureItem|GalleryImage)\\s*\\{" 'src/app/[subdomain]/preview-client.tsx'` outputs `0` (all 5 local interface declarations deleted).
    - `grep -cE "^function (isColorDark|isLightColor)\\(" 'src/app/[subdomain]/preview-client.tsx'` outputs `0` (both local utility duplicates deleted).
    - `grep -c "const mockData" 'src/app/[subdomain]/preview-client.tsx'` outputs `0`.
    - `grep -c "mockData\\." 'src/app/[subdomain]/preview-client.tsx'` outputs `0` (no remaining property reads — Task 2 removes the rest by deleting PreviewHero).
    - `grep -c "from '@/components/preview/types'" 'src/app/[subdomain]/preview-client.tsx'` outputs `1` (single consolidated import).
    - The consolidated import contains all needed names: `grep -E "(Section|ServerData|StatsServer|FeatureItem|GalleryImage|isColorDark|isLightColor)" 'src/app/[subdomain]/preview-client.tsx' | grep -c "@/components/preview/types"` is at least `1` (single import line lists all 7).
    - `wc -l < 'src/app/[subdomain]/preview-client.tsx'` outputs at most `900` after Task 1 completes (was 947; delta ≥ 45 lines from this task alone).
    - `npx tsc --noEmit` produces no new errors compared to baseline (after Task 2 completes — see Step 5 ordering note).
  </acceptance_criteria>
  <done>Local type duplicates and utility duplicates removed, mockData deleted, single import from @/components/preview/types provides all needed types and helpers, line count down ≥45.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the hero case to SECTION_REGISTRY and delete the local PreviewHero function</name>
  <files>src/app/[subdomain]/preview-client.tsx</files>
  <read_first>
    - src/app/[subdomain]/preview-client.tsx (FULL FILE post-Task-1 — relocate function PreviewHero and the switch case)
    - src/lib/section-registry.tsx (confirm SECTION_REGISTRY['hero'].render signature: ({ section, serverData }))
    - .planning/phases/01-foundation-extraction/01-PATTERNS.md (preview-client.tsx Change 3 and Change 4)
    - .planning/phases/01-foundation-extraction/01-RESEARCH.md (registry dispatch in preview-client.tsx code example)
  </read_first>
  <behavior>
    - Test 1: `grep -c "function PreviewHero" preview-client.tsx` outputs 0 (local PreviewHero deleted)
    - Test 2: `grep -c "import.*SECTION_REGISTRY.*from '@/lib/section-registry'" preview-client.tsx` outputs 1
    - Test 3: The switch case for 'hero' uses SECTION_REGISTRY['hero'] lookup
    - Test 4: Other switch cases (stats, features, gamemodes, discord, gallery, staff, text, default) are UNTOUCHED — they still call the local Preview* functions
    - Test 5: tsc --noEmit, lint, and build all pass with no new errors
    - Test 6: Final preview-client.tsx line count is ≤ 817 (was 947; combined Task 1+2 delta ≥ 130)
  </behavior>
  <action>
**Goal:** Replace the inline `PreviewHero` with a registry lookup; delete the now-redundant inline function.

**Step 1 — Add the SECTION_REGISTRY import**, immediately after the import block from Task 1:

```typescript
import { SECTION_REGISTRY } from '@/lib/section-registry';
```

**Step 2 — Find and delete `function PreviewHero({ section, serverData })`** — this is currently at line 109-221 (pre-Task-1). After Task 1, the line numbers shift but the function body is unchanged. Locate via `grep -n "function PreviewHero" preview-client.tsx`.

The function spans from `function PreviewHero({ section, serverData }: { section: Section; serverData: ServerData }) {` to its closing `}` (~113 lines). DELETE the entire function definition.

After deletion, the next function in the file (typically `function PreviewStats(...)`) sits adjacent to the deleted region.

**Step 3 — Replace the hero switch case** in `PreviewClient` (around line 918 pre-edit; locate via `grep -n 'case "hero"' preview-client.tsx`).

Find:

```typescript
case "hero":
  return <PreviewHero key={section.id} section={section} serverData={server} />;
```

Replace with:

```typescript
case "hero": {
  const Entry = SECTION_REGISTRY["hero"];
  return <Entry.render key={section.id} section={section} serverData={server} />;
}
```

The block-scoped `case ... { ... }` is required so `const Entry` is locally scoped (avoids a TypeScript "case fall-through declaration" warning under strict mode).

**Step 4 — Verify the other cases are unchanged**:

- `case "stats":` → `<PreviewStats ... />` (UNCHANGED)
- `case "features":` → `<PreviewFeatures ... />` (UNCHANGED)
- `case "gamemodes":` → `<PreviewGamemodes ... />` (UNCHANGED)
- `case "discord":` → `<PreviewDiscord ... />` (UNCHANGED)
- `case "gallery":` → `<PreviewGallery ... />` (UNCHANGED)
- `case "staff":` → `<PreviewStaff ... />` (UNCHANGED)
- `case "text":` → `<PreviewText ... />` (UNCHANGED)
- `default:` → fallback `<section ...>{section.type} preview</section>` (UNCHANGED)

Per D-04 (Hero only in Phase 1), these other cases stay as-is. They will be migrated in Phase 3 when their proper render components are extracted.

**Step 5 — Verify line count**:

```bash
NEW=$(wc -l < 'src/app/[subdomain]/preview-client.tsx')
echo "TOTAL_DELTA=$(($NEW - 947))"
# Task 1: ~-50, Task 2: ~-110 from PreviewHero deletion + ~5 from case body change = ~-110 net
# Combined target: ≤ 817 (delta ≤ -130)
```

**Step 6 — Run tsc, lint, and build**:

```bash
npx tsc --noEmit 2>&1 | tee /tmp/tsc-after-05.txt
npm run lint 2>&1 | tee /tmp/lint-after-05.txt
npm run build 2>&1 | tail -30
```

All three must succeed with no NEW errors compared to the baseline at the start of plan 01.

**Hard rules:**

- Only the `case "hero":` branch is touched. All other switch branches stay verbatim.
- The local `PreviewHero` function is DELETED, not commented out.
- No new imports beyond `SECTION_REGISTRY`.
- No changes to `PreviewClient`'s exported signature or its `isPreviewMode` banner JSX.
- The `key={section.id}` prop is preserved on the registry-rendered hero (React requires it on list children).
  </action>
  <verify>
  <automated>NEW=$(wc -l < 'src/app/[subdomain]/preview-client.tsx'); test $NEW -le 817 && [ "$(grep -c 'function PreviewHero' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && grep -c "import.*SECTION_REGISTRY.*from '@/lib/section-registry'" 'src/app/[subdomain]/preview-client.tsx' | grep -q "^1$" && grep -E "SECTION_REGISTRY\\[(\"|')hero(\"|')\\]" 'src/app/[subdomain]/preview-client.tsx' | wc -l | grep -qE "^[1-9]" && [ "$(grep -c 'mockData' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && [ "$(grep -c 'PreviewHero' 'src/app/[subdomain]/preview-client.tsx')" = "0" ] && grep -c 'case "stats":' 'src/app/[subdomain]/preview-client.tsx' | grep -q "^1$" && grep -c 'case "features":' 'src/app/[subdomain]/preview-client.tsx' | grep -q "^1$" && npx tsc --noEmit 2>&1 | grep -v "^$" | head -10</automated>
  </verify>
  <acceptance_criteria> - `wc -l < 'src/app/[subdomain]/preview-client.tsx'` outputs at most `817` (was 947; combined delta ≥ 130). - `grep -c "function PreviewHero" 'src/app/[subdomain]/preview-client.tsx'` outputs `0`. - `grep -c "PreviewHero" 'src/app/[subdomain]/preview-client.tsx'` outputs `0` (no references to the deleted function name remain). - `grep -c "mockData" 'src/app/[subdomain]/preview-client.tsx'` outputs `0` (Task 1 deleted the constant; Task 2 removed the last 4 references via PreviewHero deletion). - `grep -c "import.*SECTION_REGISTRY.*from '@/lib/section-registry'" 'src/app/[subdomain]/preview-client.tsx'` outputs `1`. - `grep -cE "SECTION_REGISTRY\\[(\"|')hero(\"|')\\]" 'src/app/[subdomain]/preview-client.tsx'` outputs at least `1`. - The other switch cases are intact: `for c in stats features gamemodes discord gallery staff text; do grep -c "case \"$c\":" 'src/app/[subdomain]/preview-client.tsx' | grep -q "^1$" || { echo "MISSING case $c"; exit 1; }; done` exits 0. - `grep -c "Entry.render" 'src/app/[subdomain]/preview-client.tsx'` outputs at least `1` (registry-resolved component invoked). - `npx tsc --noEmit` produces no new errors compared to baseline. - `npm run lint` produces no new errors/warnings compared to baseline. - `npm run build` exits with code 0 (full Next.js build succeeds — proves preview-client.tsx compiles into the app graph).
  </acceptance_criteria>
  <done>Local PreviewHero deleted, registry lookup wired in for the hero case, other switch cases untouched, mockData fully gone, build succeeds, line count down ≥130.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                                                                                                                                                                                                                                               |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Public site request → DOM (`[subdomain].minesites.net`) | Server data flows from DB → SSR → preview-client (client component). The Hero render path now goes through SECTION_REGISTRY → HeroRender component (extracted in plan 02). The DATA path is unchanged: server prop and sections prop still come from the same SSR loader. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                      |
| --------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-12   | Tampering              | Replacing inline PreviewHero with registry lookup could expose a different XSS surface                                         | mitigate    | The HeroRender component (plan 02) was extracted byte-for-byte from this file's PreviewHero — same JSX, same React-escaped text props, same object-form `style` for backgroundImage. No string concatenation into `dangerouslySetInnerHTML` introduced. CLAUDE.md: "No XSS risk from extracted Hero component (same behavior as before)" — verified. |
| T-01-13   | Tampering              | The registry import path `@/lib/section-registry` could be hijacked by a malicious package shadowing the alias                 | accept      | `@/` alias is project-local (resolves to `src/`). Cannot be shadowed by npm packages. Same trust model as every other `@/*` import in the codebase.                                                                                                                                                                                                  |
| T-01-14   | Information Disclosure | Removing local types in favor of imports — could the canonical types accidentally leak fields not present in the local copies? | accept      | The canonical `ServerData` adds OPTIONAL `players?`, `maxPlayers?`, `version?` fields. These do not propagate into rendered output unless explicitly consumed (e.g., HeroRender uses `serverData.players`). The Section, FeatureItem, GalleryImage, StatsServer interfaces are byte-identical to the locals. No new exposure.                        |
| T-01-15   | Repudiation            | A future regression could swap SECTION_REGISTRY['hero'].render to a malicious component without obvious source-tree change     | accept      | Registry contents are explicit and reviewed at code review (D-01 requires a single registry file with all entries listed). CI lint and TypeScript guarantees the entry exists. Same threat model as any imported component.                                                                                                                          |

</threat_model>

<verification>
After both tasks complete:
1. `wc -l < 'src/app/[subdomain]/preview-client.tsx'` outputs ≤ 817 (delta ≥ 130 from 947 baseline).
2. `grep -E "function PreviewHero|const mockData|^interface (Section|ServerData|StatsServer|FeatureItem|GalleryImage)" 'src/app/[subdomain]/preview-client.tsx' | wc -l` outputs `0`.
3. `npx tsc --noEmit` clean (no new errors vs baseline).
4. `npm run lint` clean (no new errors/warnings vs baseline).
5. `npm run build` exits 0.
6. **Visual verification (executor-performed manual smoke after build succeeds, documented in SUMMARY)**:
   - Run `npm run dev`.
   - Visit `http://localhost:3000/[any-test-subdomain]` (or the test path the project uses for the `[subdomain]` route).
   - The Hero section renders with the same alignment, background, player badge, and CTA buttons as before this plan.
   - Visit `http://localhost:3000/[any-test-subdomain]?preview=true` — Preview Mode banner shows; Hero still renders.
   - Compare with the visual baseline (a screenshot captured before this plan started, OR by stashing the changes and reloading) — pixel-equivalent rendering expected.
   - Document the result in the SUMMARY: "Visual smoke PASS — Hero pixel-equivalent before/after" or specific differences if any.
</verification>

<success_criteria>

- preview-client.tsx line count ≤ 817 (≥130 lines removed).
- Hero rendering goes through `SECTION_REGISTRY['hero'].render`.
- Local Section, ServerData, StatsServer, FeatureItem, GalleryImage interfaces are deleted; types imported from `@/components/preview/types`.
- Local isColorDark, isLightColor are deleted; imported from `@/components/preview/types`.
- mockData constant is deleted.
- Local PreviewHero function is deleted.
- Other switch cases (stats, features, gamemodes, discord, gallery, staff, text, default) are byte-unchanged.
- `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean.
- ROADMAP success criterion #1 ("Hero is registered there and renders identically to before") is satisfied at runtime — visual smoke test passes.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-extraction/01-05-SUMMARY.md` documenting:
- Baseline preview-client.tsx line count (947) → final line count (must be ≤ 817).
- Total lines removed (must be ≥ 130).
- Confirmation grep results: function PreviewHero (0), const mockData (0), local interfaces deleted (5×0), local utilities deleted (2×0).
- The exact form of the new SECTION_REGISTRY import line and the new hero case body (verbatim).
- npx tsc, npm run lint, npm run build results (all must succeed).
- Visual smoke test result (PASS / FAIL with description).
- Any deviations from the action text and why.
</output>
