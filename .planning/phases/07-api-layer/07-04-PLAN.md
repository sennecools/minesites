---
phase: 07-api-layer
plan: 04
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/app/(dashboard)/dashboard/actions.ts
  - src/app/(dashboard)/dashboard/create-server-dialog.tsx
  - src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx
  - src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/dashboard/servers/page.tsx
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03]

must_haves:
  truths:
    - "src/app/(dashboard)/dashboard/actions.ts exports createWebsite, updateWebsite, deleteWebsite (and togglePublished unchanged); the old createServer/updateServer/deleteServer names are GONE"
    - "create-server-dialog.tsx imports { createWebsite } and calls createWebsite(formData)"
    - "[serverId]/server-actions.tsx imports { togglePublished, deleteWebsite } and calls deleteWebsite(serverId)"
    - "[serverId]/server-settings.tsx imports { updateWebsite } and calls updateWebsite(...)"
    - "dashboard/page.tsx fetches /api/websites (not /api/servers)"
    - "dashboard/servers/page.tsx fetches /api/websites (not /api/servers)"
    - "All P2002 catches, session-user existence check (D-20), and uniqueness pre-checks remain intact in renamed actions"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/actions.ts"
      provides: "renamed server actions: createWebsite, updateWebsite, deleteWebsite, togglePublished"
      exports: ["createWebsite", "updateWebsite", "deleteWebsite", "togglePublished"]
  key_links:
    - from: "src/app/(dashboard)/dashboard/create-server-dialog.tsx"
      to: "src/app/(dashboard)/dashboard/actions.ts createWebsite"
      via: "named import"
      pattern: "import \\{ createWebsite \\}"
    - from: "src/app/(dashboard)/dashboard/page.tsx and dashboard/servers/page.tsx"
      to: "GET /api/websites (built by Plan 07-02)"
      via: "fetch call"
      pattern: "fetch.*\\\"/api/websites\\\""
---

<objective>
Rename the dashboard server actions per D-14 (createServer → createWebsite, updateServer → updateWebsite, deleteServer → deleteWebsite) and update every importer + every client-side fetch call from `/api/servers*` to `/api/websites*`.

Carry-forward guards (D-19 P2002 catch, D-20 user existence check) must remain inside the renamed actions verbatim — only the function names change.

Files touched:
1. `src/app/(dashboard)/dashboard/actions.ts` — rename exports.
2. `src/app/(dashboard)/dashboard/create-server-dialog.tsx` — update import and call site. (File is NOT renamed in this phase — that's Phase 8.)
3. `src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx` — update import and call site for delete.
4. `src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx` — update import and call site for update.
5. `src/app/(dashboard)/dashboard/page.tsx` — change `fetch("/api/servers")` → `fetch("/api/websites")`.
6. `src/app/(dashboard)/dashboard/servers/page.tsx` — change `fetch("/api/servers")` → `fetch("/api/websites")`.

OUT OF SCOPE for this plan:
- Editor god-component (`[serverId]/page.tsx`) — handled by Plan 07-05 (different file, separate concerns).
- Deleting `src/app/api/servers/` directory — handled by Plan 07-06.
- Deleting `src/lib/validations/server.ts` — handled by Plan 07-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-api-layer/07-CONTEXT.md
@src/app/(dashboard)/dashboard/actions.ts
@src/app/(dashboard)/dashboard/create-server-dialog.tsx
@src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx
@src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx
@src/app/(dashboard)/dashboard/page.tsx
@src/app/(dashboard)/dashboard/servers/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename server actions in actions.ts (createServer → createWebsite, updateServer → updateWebsite, deleteServer → deleteWebsite)</name>
  <files>src/app/(dashboard)/dashboard/actions.ts</files>
  <read_first>
    - src/app/(dashboard)/dashboard/actions.ts (full file — 159 lines; preserve P2002 catches D-19 and user existence check D-20)
    - .planning/phases/07-api-layer/07-CONTEXT.md §D-14, §D-19, §D-20
    - .planning/phases/06-schema-reset/06-REVIEW-FIX.md §WR-05 (P2002 pattern)
  </read_first>
  <action>
Edit `src/app/(dashboard)/dashboard/actions.ts`:

1. Rename the exported function `createServer` to `createWebsite`. ALL existing body code stays — the P2002 catch (D-19), `userRecord` existence check (D-20), `createWebsiteSchema.parse`, default Hero section, `revalidatePath`, and `redirect` MUST remain. Only the function name changes.

2. Rename the exported function `updateServer` to `updateWebsite`. ALL existing body code stays — the P2002 catch (D-19), `updateWebsiteSchema.parse`, subdomain conflict check, and `revalidatePath` calls MUST remain. Only the function name changes. Note: the first parameter is `serverId` — KEEP this parameter name unchanged because the dashboard route still uses `[serverId]` until Phase 8 (D-02).

3. Rename the exported function `deleteServer` to `deleteWebsite`. ALL existing body code stays. KEEP `serverId` parameter name.

4. Leave `togglePublished` unchanged. (It already calls `db.website.*` — no naming concern.)

5. Confirm the file still:
   - Imports `Prisma` from `@prisma/client`.
   - Catches `Prisma.PrismaClientKnownRequestError` with `code === "P2002"` in both create + update flows.
   - Contains the `userRecord` existence check before `db.website.create`.
   - Imports `createWebsiteSchema` and `updateWebsiteSchema` from `@/lib/validations/website`.

The function bodies must be byte-identical to the current implementation except for the function names on lines 10, 68, 115.

Run `npx tsc --noEmit` afterwards. Expect compile errors in the importing files — those are fixed by Tasks 2-4. tsc errors should ONLY be in importers, not in actions.ts itself.
  </action>
  <verify>
    <automated>grep -q "export async function createWebsite" "src/app/(dashboard)/dashboard/actions.ts" && grep -q "export async function updateWebsite" "src/app/(dashboard)/dashboard/actions.ts" && grep -q "export async function deleteWebsite" "src/app/(dashboard)/dashboard/actions.ts" && grep -q "export async function togglePublished" "src/app/(dashboard)/dashboard/actions.ts" && ! grep -E "export async function (createServer|updateServer|deleteServer)\b" "src/app/(dashboard)/dashboard/actions.ts" && grep -q "PrismaClientKnownRequestError" "src/app/(dashboard)/dashboard/actions.ts" && grep -q "P2002" "src/app/(dashboard)/dashboard/actions.ts" && grep -q "Session expired" "src/app/(dashboard)/dashboard/actions.ts" && grep -q "createWebsiteSchema" "src/app/(dashboard)/dashboard/actions.ts" && grep -q "updateWebsiteSchema" "src/app/(dashboard)/dashboard/actions.ts" && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function createWebsite" src/app/(dashboard)/dashboard/actions.ts` succeeds.
    - `grep -q "export async function updateWebsite" src/app/(dashboard)/dashboard/actions.ts` succeeds.
    - `grep -q "export async function deleteWebsite" src/app/(dashboard)/dashboard/actions.ts` succeeds.
    - `grep -q "export async function togglePublished" src/app/(dashboard)/dashboard/actions.ts` succeeds (unchanged).
    - Old names are gone: `! grep -E "export async function (createServer|updateServer|deleteServer)\b" src/app/(dashboard)/dashboard/actions.ts`.
    - D-19 P2002 catch retained: `grep -c "P2002" src/app/(dashboard)/dashboard/actions.ts` returns at least 2 (one in create, one in update).
    - D-20 user existence check retained: `grep -q "Session expired" src/app/(dashboard)/dashboard/actions.ts` succeeds.
    - Schema imports retained: `grep -q "createWebsiteSchema" src/app/(dashboard)/dashboard/actions.ts` and `grep -q "updateWebsiteSchema" src/app/(dashboard)/dashboard/actions.ts` both succeed.
  </acceptance_criteria>
  <done>Three functions renamed; all carry-forward guards intact; togglePublished untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update create-server-dialog.tsx and editor server-action wrapper files to call the renamed actions</name>
  <files>src/app/(dashboard)/dashboard/create-server-dialog.tsx, src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx, src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/create-server-dialog.tsx (imports createServer on line 18, calls await createServer(formData) on line 53)
    - src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx (imports { togglePublished, deleteServer } from "../actions" line 15; calls await deleteServer(serverId) line 33)
    - src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx (imports { updateServer } from "../actions" line 7; calls await updateServer(server.id, formData) line 50)
  </read_first>
  <action>
1. In `src/app/(dashboard)/dashboard/create-server-dialog.tsx`:
   - Replace `import { createServer } from "./actions";` with `import { createWebsite } from "./actions";`
   - Replace `await createServer(formData);` with `await createWebsite(formData);`
   - Leave the rest of the file untouched (preserve the `isRedirectError` CR-02 fix, `open`/`onOpenChange` props for WR-03 fix, and dialog UI).

2. In `src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx`:
   - Replace `import { togglePublished, deleteServer } from "../actions";` with `import { togglePublished, deleteWebsite } from "../actions";`
   - Replace `await deleteServer(serverId);` with `await deleteWebsite(serverId);`
   - Leave everything else untouched.

3. In `src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx`:
   - Replace `import { updateServer } from "../actions";` with `import { updateWebsite } from "../actions";`
   - Replace `await updateServer(server.id, formData);` with `await updateWebsite(server.id, formData);`
   - Leave everything else untouched.

Filename rename to `create-website-dialog.tsx` is deferred to Phase 8 per D-14.
  </action>
  <verify>
    <automated>grep -q "import { createWebsite } from \"./actions\"" "src/app/(dashboard)/dashboard/create-server-dialog.tsx" && grep -q "await createWebsite(formData)" "src/app/(dashboard)/dashboard/create-server-dialog.tsx" && ! grep -q "createServer" "src/app/(dashboard)/dashboard/create-server-dialog.tsx" && grep -q "import { togglePublished, deleteWebsite } from \"../actions\"" "src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx" && grep -q "await deleteWebsite(serverId)" "src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx" && ! grep -q "deleteServer" "src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx" && grep -q "import { updateWebsite } from \"../actions\"" "src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx" && grep -q "await updateWebsite(server.id, formData)" "src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx" && ! grep -q "updateServer" "src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx" && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { createWebsite } from \"./actions\"" src/app/(dashboard)/dashboard/create-server-dialog.tsx` succeeds.
    - `grep -q "await createWebsite(formData)" src/app/(dashboard)/dashboard/create-server-dialog.tsx` succeeds.
    - `! grep -q "createServer" src/app/(dashboard)/dashboard/create-server-dialog.tsx` (no stale references).
    - `grep -q "import { togglePublished, deleteWebsite } from \"../actions\"" src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx` succeeds.
    - `grep -q "await deleteWebsite(serverId)" src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx` succeeds.
    - `! grep -q "deleteServer" src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx` (no stale references).
    - `grep -q "import { updateWebsite } from \"../actions\"" src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx` succeeds.
    - `grep -q "await updateWebsite(server.id, formData)" src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx` succeeds.
    - `! grep -q "updateServer" src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx` (no stale references).
    - CR-02 isRedirectError guard preserved in create-server-dialog.tsx: `grep -q "isRedirectError" src/app/(dashboard)/dashboard/create-server-dialog.tsx` succeeds.
  </acceptance_criteria>
  <done>All three action importers reference the renamed exports; original CR-02 and WR-03 fixes preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Update fetch URLs in dashboard list pages from /api/servers to /api/websites</name>
  <files>src/app/(dashboard)/dashboard/page.tsx, src/app/(dashboard)/dashboard/servers/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/dashboard/page.tsx (fetch("/api/servers") on line 37)
    - src/app/(dashboard)/dashboard/servers/page.tsx (fetch("/api/servers") on line 41)
  </read_first>
  <action>
1. In `src/app/(dashboard)/dashboard/page.tsx`:
   - Replace the single occurrence of `fetch("/api/servers")` with `fetch("/api/websites")`.
   - Leave error messages, state shape, render code untouched.

2. In `src/app/(dashboard)/dashboard/servers/page.tsx`:
   - Replace the single occurrence of `fetch("/api/servers")` with `fetch("/api/websites")`.
   - Leave error messages, state shape, render code untouched.

These two pages render the website list cards and target Plan 07-02's GET /api/websites handler. No other API URLs in these two files (verify with grep).
  </action>
  <verify>
    <automated>grep -q "fetch(\"/api/websites\")" "src/app/(dashboard)/dashboard/page.tsx" && ! grep -q "/api/servers" "src/app/(dashboard)/dashboard/page.tsx" && grep -q "fetch(\"/api/websites\")" "src/app/(dashboard)/dashboard/servers/page.tsx" && ! grep -q "/api/servers" "src/app/(dashboard)/dashboard/servers/page.tsx" && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "fetch(\"/api/websites\")" src/app/(dashboard)/dashboard/page.tsx` succeeds.
    - `! grep -q "/api/servers" src/app/(dashboard)/dashboard/page.tsx` (zero stale references).
    - `grep -q "fetch(\"/api/websites\")" src/app/(dashboard)/dashboard/servers/page.tsx` succeeds.
    - `! grep -q "/api/servers" src/app/(dashboard)/dashboard/servers/page.tsx` (zero stale references).
  </acceptance_criteria>
  <done>Both list pages fetch the new endpoint; no stale URL references.</done>
</task>

</tasks>

<verification>
- All three actions renamed in `actions.ts`; togglePublished unchanged.
- Three importers (create-server-dialog, server-actions, server-settings) updated.
- Two list pages fetch `/api/websites`.
- All Phase 6 review fixes inside `actions.ts` (D-19, D-20) are preserved verbatim.
- `npx tsc --noEmit` may still error on the editor god-component (its fetch URL is updated in Plan 07-05) — that is expected and acceptable for this plan.
</verification>

<success_criteria>
1. createWebsite/updateWebsite/deleteWebsite are the new exported names.
2. All three importer files compile against the new names.
3. Both list pages fetch /api/websites.
4. CR-02 (isRedirectError) and WR-03 (controlled dialog open) fixes preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/07-api-layer/07-04-SUMMARY.md` documenting:
- Three action renames (with line refs in actions.ts)
- Three importer updates
- Two fetch URL changes
- Confirmation that D-19 P2002 catches and D-20 existence check survived the rename
</output>
