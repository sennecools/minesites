---
phase: 08-dashboard-public-site
verified: 2026-05-12T00:00:00Z
status: human_needed
score: 4/4 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Visit /dashboard in dev with at least one Website seeded; confirm the grid renders Website cards (not Server cards), each card shows name, subdomain URL `{subdomain}.minesites.net`, section count badge, and a Visit affordance"
    expected: "Card grid renders; section count matches the number of Section rows for each Website; hover reveals the Visit anchor; clicking Visit opens https://{subdomain}.minesites.net in a new tab; clicking the card body routes to /dashboard/{websiteId}"
    why_human: "Visual layout, hover affordance, click-event delivery (the REVIEW.md CR-01 nested-anchor finding may cause browser-level DOM reparenting that affects click delivery); only manual interaction can confirm both anchors behave correctly under React 19 / Next.js 16"
  - test: "Click 'New Website' to open the create-website dialog; enter a name + an EXISTING subdomain (one already used by another Website); click Create"
    expected: "Server returns Error('Subdomain is already taken') from actions.ts P2002 catch (or 409 from the API route); the error surfaces to the user in the dialog as a red error block (the dialog has `error` state + red error JSX, lines 83-87)"
    why_human: "Cross-tenant collision requires two real DB rows owned by different users (or the same user attempting collision); the validation flow is correct on paper but the UX message delivery (toast vs inline) is human-verifiable only"
  - test: "Open the editor on /dashboard/{websiteId}, click the 'Manage Servers' button in the top-bar action cluster; in the modal: add a server with name + IP + port; edit it inline; delete it via the inline confirm row"
    expected: "Modal opens with 'Connected Minecraft Servers' title; empty state shows 'No servers connected yet' with primary Add button; add persists via POST /api/websites/{websiteId}/servers; row appears on reopen; edit persists via PUT; delete persists via DELETE; toasts fire 'Server added', 'Server updated', 'Server removed'"
    why_human: "End-to-end CRUD against the Phase 7 endpoints with a real DB; toast feedback, focus management, autoFocus on Delete, modal close-discards-draft behavior all require interactive verification"
  - test: "Visit `{subdomain}.minesites.net` for a published Website in dev (or with wildcard DNS); confirm the public route renders sections from `Website.sections`; visit the same subdomain for an unpublished Website and confirm 404"
    expected: "Published Website renders SiteNav with serverName + the published sections via PreviewClient; unpublished Website returns 404 (via notFound() in src/app/[subdomain]/page.tsx); subdomain routing flows through src/middleware.ts which rewrites hostname.split('.')[0] to /[subdomain]"
    why_human: "Wildcard DNS for *.minesites.net or local /etc/hosts override required; middleware behavior under real Host header is not visible from static analysis"
---

# Phase 8: Dashboard & Public Site Verification Report

**Phase Goal:** The dashboard shows a list of the user's websites; users can create a website, manage its connected servers in the editor, and view the live public site routed by the website's custom subdomain.
**Verified:** 2026-05-12
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth                                                                                                                                                                       | Status                       | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | `/dashboard` renders a grid of Website cards showing name, subdomain URL (clickable), and section count — no Server cards.                                                  | VERIFIED (with REVIEW CR-01) | `src/app/(dashboard)/dashboard/page.tsx:147-150` renders `{servers.map((website, i) => <WebsiteCard key={website.id} website={website} index={i} />)}`. `WebsiteCard` (`src/components/dashboard/website-card.tsx`) renders `{website.name}` (line 50), `{website.subdomain}.minesites.net` (line 52), Layers badge with `{website._count.sections} sections` (line 80), and a `<a href={\`https://${website.subdomain}.minesites.net\`} target="_blank" rel="noreferrer noopener">` visit anchor (lines 100-109). REVIEW CR-01 flags the nested `<a>`/`<button>` inside outer `<Link>` as invalid HTML / hydration risk, but is advisory and does NOT block phase goal — cards do render.                                                              |
| 2   | "New Website" button opens a dialog; user enters name and subdomain; subdomain uniqueness is validated before save.                                                         | VERIFIED                     | `src/app/(dashboard)/dashboard/page.tsx:143` mounts `<CreateWebsiteDialog open={createDialogOpen} onOpenChange={setCreateDialogOpen} />`. The dialog (`create-website-dialog.tsx`) renders a Modal with Name + Subdomain + Description fields bound via `react-hook-form` + `zodResolver(createWebsiteSchema)` (regex `/^[a-z0-9-]+$/`, min/max length). On submit it calls `createWebsite` server action (line 53). The server action (`actions.ts:60-63`) catches Prisma P2002 and throws `Error("Subdomain is already taken")`; the dialog catch (line 56) writes the message to `error` state which renders as a red block (lines 83-87). Defense in depth: `route.ts:93-97` POST also returns 409 with the same message.                          |
| 3   | Website editor includes a "Servers" tab listing connected MinecraftServer records with add/edit/remove controls. (CONTEXT D-04: relaxed to "Manage Servers" modal — verify the modal exists, not a literal tab.) | VERIFIED                     | `src/app/(dashboard)/dashboard/[websiteId]/page.tsx:75` imports `ConnectionsModal`. Line 2275 declares `const [connectionsOpen, setConnectionsOpen] = useState(false)`. Lines 2581-2589 render the `<motion.button>` "Manage Servers" in the top-bar action cluster. Line 3196 mounts `<ConnectionsModal websiteId={websiteId} isOpen={connectionsOpen} onClose={...} />`. The modal (`connections-modal.tsx`, 431 lines) fetches `GET /api/websites/{websiteId}/servers` on open (line 75), supports add via `POST` (line 100), edit via `PUT` (line 120), and delete via `DELETE` (line 140). Inline edit form, inline delete confirm with autoFocus, and toast feedback for all three operations are wired.                                          |
| 4   | Visiting `[subdomain].minesites.net` renders the Website's published sections (not a Server's) — the public routing layer uses the `Website.subdomain` field.               | VERIFIED                     | `src/middleware.ts:11-28` extracts subdomain from `host` header (first dot-segment of `hostname.split('.')`), then `NextResponse.rewrite(/${subdomain}${pathname})`. `src/app/[subdomain]/page.tsx:15-24` calls `db.website.findUnique({ where: { subdomain }, include: { sections: { where: visible } } })` and `notFound()` if not published & not preview (line 27). `src/app/[subdomain]/layout.tsx:65` calls `getServerData(subdomain)` (a cached `db.website.findUnique`) to inject the theme + SiteNav `serverName`. The `Server` model is absent (Phase 6 schema reset confirmed by grep — zero `serverIp`/`serverPort` in src/, zero `CreateServerDialog` references). |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact                                                                          | Expected                                                                                  | Status      | Details                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/app/api/websites/route.ts`                                                   | GET includes `_count.sections`; POST preserves P2002 + user-existence + apiErrorResponse  | VERIFIED    | GET select block line 27 contains `_count: { select: { sections: true } }`; POST handler lines 41-111 retain `D-20: session user existence check` (line 49-58), `createWebsiteSchema.safeParse` (line 61), P2002 catch with 409 "Subdomain is already taken" (line 97), `apiErrorResponse` (lines 33, 107). |
| `src/components/dashboard/website-card.tsx`                                       | Shared card; exports `WebsiteCard` + `WebsiteCardData`; renders name/subdomain/sections   | VERIFIED    | 115 lines (≥ 60). `export function WebsiteCard` + `export interface WebsiteCardData` present. `_count.sections` rendered twice (Badge body + aria-label). Visit anchor: target=_blank, rel=noreferrer noopener, e.stopPropagation. `p-6 rounded-2xl`. Zero `font-medium`. |
| `src/components/dashboard/index.ts`                                               | Barrel: re-exports WebsiteCard, WebsiteCardData, ConnectionsModal                         | VERIFIED    | Two named re-exports.                                                                                                                                                                                                                                                                                          |
| `src/app/(dashboard)/dashboard/create-website-dialog.tsx`                         | Renamed file (D-13 full); exports CreateWebsiteDialog; bound to createWebsiteSchema       | VERIFIED    | File exists; `export function CreateWebsiteDialog` (line 25); `interface CreateWebsiteDialogProps` (line 20); `useForm({ resolver: zodResolver(createWebsiteSchema) })` (line 41); old `create-server-dialog.tsx` does NOT exist (verified by ls). Zero `CreateServerDialog` references anywhere in `src/`. |
| `src/app/(dashboard)/dashboard/page.tsx`                                          | Consumes `<WebsiteCard />` + `<CreateWebsiteDialog />`; WebsiteData has `_count.sections` | VERIFIED    | Imports `WebsiteCard` from `@/components/dashboard` (line 15) and `CreateWebsiteDialog` from `./create-website-dialog` (line 14). Local `WebsiteData` includes `_count: { sections: number }` (line 25). Grid renders `<WebsiteCard />` (line 149). `<CreateWebsiteDialog />` mounted at line 143.            |
| `src/app/(dashboard)/dashboard/servers/page.tsx`                                  | Consumes `<WebsiteCard />` + `<CreateWebsiteDialog />` for grid; list table preserved     | VERIFIED    | Imports `WebsiteCard` + `CreateWebsiteDialog` (lines 16-17). `WebsiteData._count: { sections: number }` (line 28). Grid renders `<WebsiteCard />` (line 172). `filteredServers.map` used twice (grid + list-view table at line 208 — table preserved).                                                       |
| `src/components/dashboard/connections-modal.tsx`                                  | Per-record CRUD over REST; ≥ 200 lines; no isRedirectError                                | VERIFIED    | 431 lines. `export function ConnectionsModal` (line 44). All four REST verbs hit `/api/websites/{websiteId}/servers[/serverId]`. `body.error ??` unwrap appears 4× (one per CRUD). Zero `isRedirectError`. Zero `font-medium`. `zodResolver(createMcserverSchema)`. `className="max-w-lg"`. Copy verbatim. |
| `src/app/(dashboard)/dashboard/[websiteId]/page.tsx`                              | Editor god-component; ConnectionsModal mounted; params.websiteId destructure              | VERIFIED    | `params.websiteId as string` (line 2244). `const [connectionsOpen, setConnectionsOpen] = useState(false)` (line 2275). "Manage Servers" button (lines 2581-2589, `font-normal` per UI-SPEC). `<ConnectionsModal websiteId={websiteId} isOpen={connectionsOpen} onClose={...} />` (line 3196). File size 3199 lines. Zero non-comment `serverId` references. |
| `src/app/[subdomain]/page.tsx`                                                    | RSC fetches by Website.subdomain; notFound() for drafts                                   | VERIFIED    | Line 15-24: `db.website.findUnique({ where: { subdomain } })` with sections include. Line 27: `if (!server || (!server.published && !isPreviewMode)) notFound();`. `serverData` literal (lines 32-35) is `{ name, subdomain }` only (zero `serverIp`).                                                       |
| `src/app/[subdomain]/layout.tsx`                                                  | `.site-root` wrapper; SiteNav single-prop; no serverIp                                    | VERIFIED    | `getServerData` cached `db.website.findUnique` (line 51). `<SiteNav serverName={serverName} />` (line 105). `.site-root` wrapper preserved (line 101). Zero `serverIp` references. CSS-var injection intact (lines 90-97).                                                                                  |
| `src/components/preview/types.ts`                                                 | `WebsiteData` has no `serverIp`                                                           | VERIFIED    | `WebsiteData` (lines 3-9): `name`, `subdomain`, optional `players`/`maxPlayers`/`version` only. Grep confirms zero `serverIp` in the file.                                                                                                                                                                  |
| `src/middleware.ts`                                                               | Subdomain rewrite to `/[subdomain]` route                                                 | VERIFIED    | Lines 17-28: extracts `parts[0]` from `hostname.split('.')`, rewrites pathname to `/${subdomain}${pathname}` via `NextResponse.rewrite(url)`. Auth gate for `/dashboard` preserved (lines 31-37).                                                                                                            |

### Key Link Verification

| From                                       | To                                                                            | Via                                            | Status     | Details                                                                                                                                                                                          |
| ------------------------------------------ | ----------------------------------------------------------------------------- | ---------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `dashboard/page.tsx`                       | `/api/websites`                                                               | `fetch` in `useEffect` on mount                | WIRED      | Line 37: `await fetch("/api/websites")` → `setServers(data)` (line 42). `useEffect` dependency `[]`. Response includes `_count.sections` (verified at API layer).                                |
| `dashboard/servers/page.tsx`               | `/api/websites`                                                               | `fetch` in `useEffect` on mount                | WIRED      | Line 42: same pattern as `/dashboard`. Both list pages share the same data flow.                                                                                                                  |
| `website-card.tsx`                         | `https://{subdomain}.minesites.net`                                           | `<a target="_blank" rel="noreferrer noopener" onClick={stopPropagation}>` | WIRED | Lines 100-109. Reverse-tab-nabbing mitigation present; click-bubbling mitigation present.                                                                                                         |
| `website-card.tsx`                         | `/dashboard/{website.id}`                                                     | `<Link href={...}>` wrapper                    | WIRED      | Line 31: `<Link href={\`/dashboard/${website.id}\`}>`. URL pattern matches the renamed `[websiteId]/` route.                                                                                       |
| `create-website-dialog.tsx`                | `createWebsite` server action (subdomain uniqueness validated)                | `formData` submit                              | WIRED      | Line 53: `await createWebsite(formData)`. The server action (`actions.ts:60`) catches P2002 and throws "Subdomain is already taken" which surfaces via dialog catch (line 56) into `error` state. |
| `connections-modal.tsx`                    | `/api/websites/{websiteId}/servers` (GET on open, POST on add)                | `fetch` in `useEffect` + `addServer`           | WIRED      | Effect gated on `isOpen` (line 64-69, also clears local state on close). Cancellation cleanup via `cancelled` flag (line 71-93). POST line 100.                                                  |
| `connections-modal.tsx`                    | `/api/websites/{websiteId}/servers/{serverId}` (PUT on edit, DELETE on confirm) | `fetch` with method                          | WIRED      | PUT line 120-128; DELETE line 140-146. Both unwrap `body.error` from non-OK and call `toast(..., "error")`.                                                                                       |
| `[websiteId]/page.tsx`                     | `connections-modal.tsx`                                                       | Import + JSX mount in top-bar action cluster   | WIRED      | Import line 75. Button mount lines 2581-2589 (between hasUnsavedChanges pill and Preview button). Modal mount line 3196.                                                                          |
| `middleware.ts`                            | `/[subdomain]` App Router route                                               | `NextResponse.rewrite`                         | WIRED      | Lines 24-28: `url.pathname = \`/${subdomain}${pathname}\`` → rewrite. Matches the App Router `src/app/[subdomain]/` directory.                                                                    |
| `[subdomain]/page.tsx`                     | `db.website.findUnique({ where: { subdomain } })`                             | Prisma query                                   | WIRED      | Lines 15-24. Includes sections filtered by `visible` (unless preview mode).                                                                                                                       |
| `[subdomain]/layout.tsx`                   | `<SiteNav serverName={...} />`                                                | JSX prop pass                                  | WIRED      | Line 105. Single-prop interface confirmed.                                                                                                                                                        |

### Data-Flow Trace (Level 4)

| Artifact                                  | Data Variable           | Source                                                  | Produces Real Data | Status   |
| ----------------------------------------- | ----------------------- | ------------------------------------------------------- | ------------------ | -------- |
| `dashboard/page.tsx` Website grid         | `servers` state         | `fetch("/api/websites")` → DB findMany with `_count`    | Yes (DB query)     | FLOWING  |
| `dashboard/servers/page.tsx` Website grid | `servers` state         | Same as above                                           | Yes (DB query)     | FLOWING  |
| `website-card.tsx` section count Badge    | `website._count.sections` | Prop from parent; sourced from Prisma `_count` aggregate | Yes                | FLOWING  |
| `website-card.tsx` visit anchor href      | `website.subdomain`     | Prop; sourced from `Website.subdomain` column           | Yes                | FLOWING  |
| `connections-modal.tsx` server list       | `servers` state         | `GET /api/websites/{websiteId}/servers` (Phase 7 endpoint) | Yes (DB query, Phase 7 verified) | FLOWING |
| `[subdomain]/page.tsx` sections render    | `sections` array        | `db.website.findUnique` with sections include            | Yes                | FLOWING  |
| `[subdomain]/layout.tsx` SiteNav serverName | `server.name` from `getServerData` | `db.website.findUnique` (React.cache-deduped) | Yes                | FLOWING  |

All artifacts that render dynamic data are backed by real Prisma queries. No hardcoded empties, no static fallbacks, no "coming soon" placeholders in the verified surface.

### Behavioral Spot-Checks

| Behavior                                          | Command                                                     | Result            | Status |
| ------------------------------------------------- | ----------------------------------------------------------- | ----------------- | ------ |
| TypeScript compiles cleanly across the full diff  | `npx tsc --noEmit`                                          | exit 0 (no output) | PASS   |
| Old `[serverId]` route directory absent           | `test ! -d "src/app/(dashboard)/dashboard/[serverId]"`      | succeeds          | PASS   |
| Old `create-server-dialog.tsx` absent             | `ls "src/app/(dashboard)/dashboard/create-server-dialog.tsx"` | not found       | PASS   |
| `CreateServerDialog` purged from src/             | `grep -rn CreateServerDialog src/`                          | zero hits         | PASS   |
| `serverIp`/`serverPort` purged from src/          | `grep -rn 'serverIp\|serverPort' src/app src/components src/lib` | zero hits     | PASS   |
| Visit anchor wiring (target=_blank + rel)         | `grep -n 'noreferrer noopener\|target="_blank"' website-card.tsx` | both present | PASS   |
| ConnectionsModal mount in editor                  | `grep -n '<ConnectionsModal' [websiteId]/page.tsx`           | line 3196        | PASS   |
| Subdomain routing in middleware                   | `grep -n 'NextResponse.rewrite' src/middleware.ts`           | line 27          | PASS   |

### Requirements Coverage

| Requirement | Source Plan         | Description                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                       |
| ----------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| DASH-01     | 08-03               | Main dashboard page shows the user's Websites instead of Servers                                                                     | SATISFIED  | Both list pages render `<WebsiteCard />` (no Server cards). `WebsiteData` interface is the type; data flows from `/api/websites` → grid render. REQUIREMENTS.md description matched.                                          |
| DASH-02     | 08-03               | Dashboard includes a "New Website" button/dialog where user picks a name and custom subdomain                                        | SATISFIED  | `CreateWebsiteDialog` renamed (D-13 full); subdomain regex validation client-side; server-side P2002 catch with "Subdomain is already taken" error message surfaces via dialog catch → error state → red error JSX block. |
| DASH-03     | 08-01 (rename), 08-04 (modal) | Website editor includes a server connections manager tab for adding, editing, and removing connected Minecraft servers              | SATISFIED  | Per CONTEXT D-04 relaxed from "tab" to "Manage Servers" modal opened from editor top-bar — capability is identical. Modal supports add/edit/remove via per-record REST against Phase 7 endpoints. NOTE: Plan 08-01 frontmatter claims DASH-03 (route rename), Plan 08-04 frontmatter claims DASH-03 (modal); both contribute. The modal is the dominant artifact. |
| DASH-04     | 08-02 (public path), 08-03 (card live URL) | Website cards in the dashboard list display the live URL (e.g., myhardcoreserver.minesites.net)                                     | SATISFIED  | `WebsiteCard` line 52 renders `{website.subdomain}.minesites.net` as muted subtitle; line 101 renders the same as a clickable `target="_blank"` anchor. Public routing layer (`[subdomain]/page.tsx` + middleware) uses `Website.subdomain`. Plan 08-02 verified the public path renders post-`serverIp` purge. |

All four declared requirement IDs are SATISFIED. No orphaned requirements identified.

### Anti-Patterns Found

| File                                                    | Line       | Pattern                                       | Severity | Impact                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------- | ---------- | --------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/dashboard/website-card.tsx`             | 31-113     | Nested interactive elements (a > button + a > a) | Warning  | REVIEW.md CR-01 (BLOCKER per the reviewer, but advisory per orchestrator). HTML5 spec violates content-model rule; browsers reparent the inner `<a>` causing hydration mismatch and potentially incorrect click delivery on the outer Link. The cards render and the visit handler uses `stopPropagation` to compensate, so the phase goal (cards render with name, subdomain URL, section count) is achieved. Recommend fix in a follow-up phase. |
| (Other warnings)                                        | various    | See 08-REVIEW.md                              | Warning  | 7 warning-class findings: dead code, ARIA regressions, an orphaned section component with potential security hazard. None block the four ROADMAP success criteria.                                                                                                                                                                                                |

No blocker anti-patterns relative to the phase goal. REVIEW.md CR-01 is a code-quality blocker (invalid HTML, hydration risk) but the success criterion 1 reads "renders a grid of Website cards showing name, subdomain URL (clickable), and section count" — the cards do render and the data does flow. The nested-anchor issue is a follow-up correction, not a goal failure.

### Human Verification Required

1. **Dashboard Website grid + Visit anchor click delivery**

   **Test:** Visit `/dashboard` in dev with at least one Website seeded; confirm the grid renders Website cards, each card shows name, subdomain URL `{subdomain}.minesites.net`, section count badge, and a Visit affordance.
   **Expected:** Card grid renders; section count matches DB; hover reveals the Visit anchor; clicking Visit opens the public URL in a new tab; clicking the card body routes to `/dashboard/{websiteId}`.
   **Why human:** Visual layout, hover affordance, and click-event delivery interact with the REVIEW CR-01 nested-anchor issue. Only interactive testing can confirm both anchors behave correctly under React 19 / Next.js 16 hydration.

2. **Subdomain uniqueness validation UX**

   **Test:** Click "New Website" to open the create-website dialog; enter a name + an existing subdomain (one already used by another Website); click Create.
   **Expected:** Server throws `Error("Subdomain is already taken")` via P2002 catch; dialog surfaces the message in the red error JSX block.
   **Why human:** Cross-tenant collision requires two real DB rows; the validation flow is correct on paper but UX message delivery is human-verifiable only.

3. **Manage Servers end-to-end CRUD**

   **Test:** Open the editor on `/dashboard/{websiteId}`, click "Manage Servers"; add a server (name + IP + port); edit it inline; delete it via the inline confirm row.
   **Expected:** Modal opens with title "Connected Minecraft Servers"; empty state shows the primary Add button; add persists via POST; row appears on reopen; edit persists via PUT; delete persists via DELETE; toasts fire for each success.
   **Why human:** End-to-end CRUD against Phase 7 endpoints with a real DB; toast feedback, focus management (autoFocus on Delete), and modal close-discards-draft behavior require interactive verification.

4. **Subdomain public routing**

   **Test:** Visit `{subdomain}.minesites.net` for a published Website (or with wildcard DNS / `/etc/hosts`); visit the same subdomain for an unpublished Website.
   **Expected:** Published Website renders SiteNav + PreviewClient sections; unpublished Website returns 404.
   **Why human:** Wildcard DNS or local `/etc/hosts` override required; middleware behavior under real Host header is not visible from static analysis.

### Gaps Summary

No gaps relative to the phase goal. All four ROADMAP success criteria are satisfied by the codebase:

- DASH-01: Website cards render in a grid; no Server cards exist.
- DASH-02: CreateWebsiteDialog (D-13 full rename) opens with name + subdomain inputs; server-side P2002 catch validates uniqueness and surfaces "Subdomain is already taken" via dialog error JSX.
- DASH-03: ConnectionsModal (CONTEXT D-04 relaxed from tab to modal) supports add/edit/remove against Phase 7 per-record REST endpoints; mounted in editor top-bar.
- DASH-04: WebsiteCard shows the live URL twice (subtitle + Visit anchor); public route at `/[subdomain]` flows through middleware subdomain extraction → `db.website.findUnique({ where: { subdomain } })` → published-or-preview gate → PreviewClient render.

TypeScript compile gate passes (exit 0). The legacy `serverId`/`serverIp`/`serverPort`/`CreateServerDialog` surface is fully purged. The four human verification items above are real-world flow validations the phase can only confirm in a running environment.

REVIEW.md CR-01 (nested anchor in WebsiteCard) is a code-quality issue worth fixing in a follow-up plan but does not block phase goal achievement — the cards render with all required data.

---

_Verified: 2026-05-12_
_Verifier: Claude (gsd-verifier)_
