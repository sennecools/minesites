---
phase: 08-dashboard-public-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/dashboard/[serverId]/page.tsx
  - src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx
  - src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx
autonomous: true
requirements: [DASH-03]
must_haves:
  truths:
    - "Editor route param is named websiteId, not serverId"
    - "Editor still loads when navigated to /dashboard/{id} (URL pattern unchanged)"
    - "TypeScript compiles after the rename with zero errors"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/[websiteId]/page.tsx"
      provides: "Editor god-component at new route directory"
      contains: "useParams<{ websiteId: string }>()"
    - path: "src/app/(dashboard)/dashboard/[websiteId]/server-actions.tsx"
      provides: "Sibling actions module (renamed types)"
    - path: "src/app/(dashboard)/dashboard/[websiteId]/server-settings.tsx"
      provides: "Sibling settings component (renamed types, dropped serverIp/serverPort)"
      contains: "interface Website"
  key_links:
    - from: "src/app/(dashboard)/dashboard/[websiteId]/page.tsx"
      to: "/api/websites/{websiteId}"
      via: "fetch in useEffect on mount"
      pattern: "fetch\\(`/api/websites/\\$\\{websiteId\\}`"
---

<objective>
Rename the editor route directory `[serverId]/` -> `[websiteId]/` and sweep the local `serverId` token + `Server*` type names inside the god-component and its two siblings. This is the "directory rename first in its own commit" step per the user's CONTEXT specifics so a regression bisect cleanly attributes any breakage.

Purpose: Closes the v1.1 terminology cleanup (D-12, D-15). All public-facing URL patterns are unchanged (`/dashboard/{id}` still works) — only the route param name changes. Also drops the legacy `serverIp`/`serverPort` fields from the local `Server` interface in `server-settings.tsx` (D-14).

Output:
- Directory renamed
- 5 `serverId` references in `page.tsx` swept to `websiteId`
- 4 `serverId` references in `server-actions.tsx` swept (local-type/destructure consistency per D-15)
- `Server` interface in `server-settings.tsx` -> `Website` interface, with `serverIp` + `serverPort` dropped per D-14
- Build cache cleared (`.next/`) and TypeScript compile gate passes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dashboard-public-site/08-CONTEXT.md
@.planning/phases/08-dashboard-public-site/08-RESEARCH.md
@.planning/phases/08-dashboard-public-site/08-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Current state of the godcomponent's useParams + fetch shape (lines verified 2026-05-12). -->
<!-- After this plan, every occurrence of `serverId` inside [websiteId]/* becomes `websiteId`. -->

From src/app/(dashboard)/dashboard/[serverId]/page.tsx (current, to be renamed):
```typescript
import { useParams } from "next/navigation";
// ...
const params = useParams();
const serverId = params.serverId as string;            // line 2243
// ...
const response = await fetch(`/api/websites/${serverId}`);  // line 2279
// ...
}, [serverId]);                                        // line 2331
const response = await fetch(`/api/websites/${serverId}`, { method: "PUT", ... });  // line 2377
// ...
}, [serverId, serverData, navbarSettings, themeSettings, sections]);  // line 2425
```

From src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx (current, to drop serverIp/serverPort):
```typescript
interface Server {
  id: string;
  name: string;
  subdomain: string;
  description: string | null;
  serverIp: string | null;   // DROP per D-14
  serverPort: number | null; // DROP per D-14
}

interface ServerSettingsProps {
  server: Server;
}
```
(File body lines 84-126 render only name/subdomain/description — no `serverIp` JSX exists. Drop the type fields only.)

From src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx (orphaned but in scope for D-15):
```typescript
// Contains local Server* type references. Apply the same rename heuristic
// — local Server type names become Website*. Do NOT delete the file; leave
// orphaned + renamed per RESEARCH §State of the Art recommendation.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directory and sweep param + type names</name>
  <files>
    src/app/(dashboard)/dashboard/[websiteId]/page.tsx,
    src/app/(dashboard)/dashboard/[websiteId]/server-actions.tsx,
    src/app/(dashboard)/dashboard/[websiteId]/server-settings.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/dashboard/[serverId]/page.tsx (lines 1-50 imports + 2240-2260 useParams block + 2275-2335 first fetch + 2370-2430 second fetch + 2920 SectionPreview call site)
    - src/app/(dashboard)/dashboard/[serverId]/server-settings.tsx (full file, especially lines 10-17 Server interface and lines 40-60 BL-06 description round-trip)
    - src/app/(dashboard)/dashboard/[serverId]/server-actions.tsx (full file)
    - .planning/phases/08-dashboard-public-site/08-CONTEXT.md (D-12, D-14, D-15, D-16)
    - .planning/phases/08-dashboard-public-site/08-PATTERNS.md §`[websiteId]/page.tsx` and §`server-settings.tsx`
    - .planning/phases/08-dashboard-public-site/08-RESEARCH.md §Pitfall 1, §Pitfall 8, §Runtime State Inventory
  </read_first>
  <action>
    Execute the rename + sweep in this exact order:

    **(a) Directory rename via git mv** so history follows:
    ```bash
    git mv "src/app/(dashboard)/dashboard/[serverId]" "src/app/(dashboard)/dashboard/[websiteId]"
    ```

    **(b) In `src/app/(dashboard)/dashboard/[websiteId]/page.tsx` — token sweep (5 hits):**
    - Line ~2242-2243:
      ```diff
      -  const params = useParams();
      -  const serverId = params.serverId as string;
      +  const params = useParams();
      +  const websiteId = params.websiteId as string;
      ```
    - Line ~2279: `fetch(\`/api/websites/${serverId}\`)` -> `fetch(\`/api/websites/${websiteId}\`)`
    - Line ~2331: `}, [serverId]);` -> `}, [websiteId]);`
    - Line ~2377: `fetch(\`/api/websites/${serverId}\`, { method: "PUT", ... })` -> `fetch(\`/api/websites/${websiteId}\`, ...)`
    - Line ~2425: `}, [serverId, serverData, navbarSettings, themeSettings, sections]);` -> `}, [websiteId, serverData, navbarSettings, themeSettings, sections]);`
    - Also: rename any local `type ServerDataState` / `interface Server*` block (per D-15) — observed at lines ~2245-2254 (`ServerDataState` -> `WebsiteDataState`). Apply globally inside this file only: any local `Server*` type alias name becomes `Website*`. Do NOT touch import names from Prisma (those are already `Website` / `MinecraftServer`) and do NOT touch React component prop names already correct.

    Net-line cap: this task must NOT grow `page.tsx`. The diff is rename-only (no logic added). Net delta target: 0 lines (rename only) — per CLAUDE.md rule 1 (the 15-line budget is reserved for the Plan 04 modal mount).

    **(c) In `src/app/(dashboard)/dashboard/[websiteId]/server-actions.tsx` — type rename (per D-15):**
    - Any local `Server*` type alias name -> `Website*`. The file is orphaned (no importers — verified by Phase 8 research) but rename for consistency. Do NOT delete the file; do NOT rename the filename (D-16 is explicitly deferred for this plan).
    - 4 `serverId` text occurrences may include local variable names — sweep to `websiteId` for consistency.

    **(d) In `src/app/(dashboard)/dashboard/[websiteId]/server-settings.tsx` — type rename + drop legacy fields (D-14 + D-15):**
    ```diff
    -interface Server {
    +interface Website {
       id: string;
       name: string;
       subdomain: string;
       description: string | null;
    -  serverIp: string | null;
    -  serverPort: number | null;
     }

    -interface ServerSettingsProps {
    -  server: Server;
    -}
    +interface ServerSettingsProps {
    +  server: Website;
    +}
    ```
    Do NOT modify the form JSX body (lines ~84-126) — it doesn't render `serverIp`/`serverPort`, and the BL-06 description round-trip block (lines ~49-57 — the `key === "description"` branch that sends `""` for explicit clear) MUST be preserved verbatim. Per D-16, do NOT rename `server-settings.tsx` -> `website-settings.tsx` (filename rename deferred to keep diff small).

    **(e) Clear Next.js build cache (per RESEARCH §Pitfall 8 + Runtime State Inventory):**
    ```bash
    rm -rf .next
    ```

    **(f) DO NOT modify any of these files in this plan** (they belong to other plans):
    - `src/app/(dashboard)/dashboard/page.tsx` (Plan 03)
    - `src/app/(dashboard)/dashboard/servers/page.tsx` (Plan 03)
    - `src/app/(dashboard)/dashboard/create-server-dialog.tsx` (Plan 03)
    - `src/app/[subdomain]/*` (Plan 02)
    - `src/components/preview/types.ts` (Plan 02)
    - `src/components/site/nav.tsx` (Plan 02)
    - The "Manage Servers" button and `<ConnectionsModal />` mount are added in Plan 04.

    The `<Link href="/dashboard/${id}">` callers in other files (8 hits across the codebase, per RESEARCH §Runtime State Inventory) pass the id as a VALUE and do NOT reference the param name — they continue to work without modification.
  </action>
  <verify>
    <automated>
    # 1. Old directory must not exist
    test ! -d "src/app/(dashboard)/dashboard/[serverId]"
    # 2. New directory must exist with all three files
    test -f "src/app/(dashboard)/dashboard/[websiteId]/page.tsx" && \
    test -f "src/app/(dashboard)/dashboard/[websiteId]/server-actions.tsx" && \
    test -f "src/app/(dashboard)/dashboard/[websiteId]/server-settings.tsx"
    # 3. Zero `serverId` occurrences inside the renamed page.tsx (comments excluded)
    test "$(grep -v '^[[:space:]]*//' 'src/app/(dashboard)/dashboard/[websiteId]/page.tsx' | grep -c 'serverId')" = "0"
    # 4. `websiteId` destructure pattern present in page.tsx
    grep -q "params.websiteId as string" "src/app/(dashboard)/dashboard/[websiteId]/page.tsx"
    # 5. server-settings.tsx interface renamed and legacy fields dropped
    grep -q "^interface Website" "src/app/(dashboard)/dashboard/[websiteId]/server-settings.tsx"
    test "$(grep -c 'serverIp\|serverPort' 'src/app/(dashboard)/dashboard/[websiteId]/server-settings.tsx')" = "0"
    # 6. TypeScript compile gate
    npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - Directory `[serverId]/` no longer exists; `[websiteId]/` exists with the three files.
    - `grep -c "serverId" src/app/(dashboard)/dashboard/[websiteId]/page.tsx` returns 0 (counting non-comment lines).
    - `useParams<{ websiteId: string }>()` is the destructure shape OR the typed-as-string assertion `params.websiteId as string` is present.
    - `interface Server { ... }` no longer exists in `server-settings.tsx`; `interface Website { ... }` exists in its place; `serverIp` and `serverPort` fields are absent.
    - BL-06 description round-trip preserved (key === "description" branch sending "" still present in server-settings.tsx).
    - `.next/` build cache was cleared after rename.
    - `npx tsc --noEmit` exits with code 0.
  </done>
  <acceptance_criteria>
    - `test ! -d "src/app/(dashboard)/dashboard/[serverId]"` returns success.
    - `ls "src/app/(dashboard)/dashboard/[websiteId]/"` lists at least: `page.tsx`, `server-actions.tsx`, `server-settings.tsx`.
    - `grep -v '^[[:space:]]*//' "src/app/(dashboard)/dashboard/[websiteId]/page.tsx" | grep -c "serverId"` returns 0.
    - `grep -c "params.websiteId" "src/app/(dashboard)/dashboard/[websiteId]/page.tsx"` returns >= 1.
    - `grep -c "^interface Website" "src/app/(dashboard)/dashboard/[websiteId]/server-settings.tsx"` returns 1.
    - `grep -c "serverIp\|serverPort" "src/app/(dashboard)/dashboard/[websiteId]/server-settings.tsx"` returns 0.
    - `grep -c 'key === "description"' "src/app/(dashboard)/dashboard/[websiteId]/server-settings.tsx"` returns >= 1 (BL-06 carry-forward preserved).
    - `npx tsc --noEmit` exits 0.
    - `git status` shows the rename as a path move (not delete+add).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard client -> /api/websites/[websiteId] | Authenticated session cookie. Phase 7 endpoints verify ownership via `userId === session.user.id`; the rename does not change this contract — `websiteId` is just the new param name for the same record-id pathway. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `useParams<{ serverId }>()` left stale after rename | mitigate | Sweep every `serverId` occurrence to `websiteId`. Verify: `grep -c "serverId" [websiteId]/page.tsx` returns 0. Per RESEARCH §Pitfall 1, stale `params.serverId` becomes `undefined` at runtime and turns the fetch into `/api/websites/undefined` — a broken-but-not-exploitable 404. |
| T-08-02 | DoS | Stale Next.js build cache serving the old route directory | mitigate | `rm -rf .next` after the rename. Per RESEARCH §Pitfall 8. |
| T-08-03 | Information disclosure | BL-06 description round-trip regression (description field silently dropped on save) | mitigate | Preserve the `key === "description"` branch in `server-settings.tsx` verbatim. Acceptance criteria explicitly verifies this. |
| T-08-04 | Information disclosure | Phase 7 carry-forward regression (subdomain 409, P2002 catch, freemium gate, user-existence check, ownership chain) | accept | This plan does NOT touch any API route or `actions.ts`. The carry-forwards live server-side and are out of this plan's diff surface. Plan 03 + Plan 04 will re-verify on the routes they touch. |
</threat_model>

<verification>
- Old directory absent: `test ! -d "src/app/(dashboard)/dashboard/[serverId]"`.
- New directory complete: page.tsx + server-actions.tsx + server-settings.tsx all present.
- No stale `serverId` references in the renamed files (comments excluded).
- TypeScript compiles cleanly.
- Existing `Link href="/dashboard/${id}"` callers continue to work (URL pattern unchanged).
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0.
- Manual smoke (executor's discretion): visiting `/dashboard/{any-existing-website-id}` in dev loads the editor (the `useParams` rename is functionally validated).
- `git diff --stat` shows the directory rename as a path move + 3 modified files; no other files touched.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-public-site/08-01-SUMMARY.md` covering:
- Directory + token sweep delta
- BL-06 preservation evidence
- `.next/` cache cleared
- TypeScript gate result
- Any unexpected `serverId` hits found and resolved
</output>
